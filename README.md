# Email-Verifier
This project has an accumulation simple and small tasks which in turn accomplishes a sophesticated larger task. This app first verifies a user's email-id, it send an OTP to verify a user's email. If otp verification is successful then a copy of the desired notes is sent to the same email-id. The libraries used in this project are Tkinter, PIL, math, random, smtp and email. 
